# Model Report

**Status: not generated yet - no real sensor data has been collected/labeled.**

This file is overwritten by `python -m src.evaluate` once a model has been trained on real data
(`python -m src.train`). Every number in the generated report comes from that run; nothing is
filled in by hand.

A report produced from the **synthetic demo dataset** (to show what the output looks like) is at
`reports/demo/model_report.md` after running `python -m src.train --demo && python -m src.evaluate --demo`.
Demo numbers are not project results.
